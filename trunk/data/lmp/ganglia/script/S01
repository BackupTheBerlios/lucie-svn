#!/usr/bin/cfengine

control:
        access = ( root )
        sysadm = ( root )

        actionsequence = (
                editfiles
        )

editfiles:
	any::
	{ /tmp/target/etc/gmond.conf
		AppendIfNoSuchLine "trusted_hosts %%%TRUSTED_HOSTS%%%"
	}
